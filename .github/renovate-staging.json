{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"description": "Update versions of dependencies in `prod` to the current version deployed into `staging`",
	"packageRules": [
		{
			"matchBaseBranches": ["prod"],
			"description": "nginx",
			"matchDepNames": ["nginx"],
			"allowedVersions": "<=1.27.0"
		}
	]
}
